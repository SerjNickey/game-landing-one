import "./DGameSafeOpened.css";

/**
 * Opened-safe prize screen (post-unlock).
 * Autonomous — no dial / hold logic.
 */
export const DGameSafeOpened = () => {
  const el = document.createElement("div");
  el.className = "game-safe-opened__container";
  return el;
};
